Qu'est-ce que l'interface kernel?

C'est une page memoire qui contient toute les informations sur le kernel,
l api ainsi que sur une multitude de détail telque :
- processor info
- memory info
- thread info 
- etc.

Le Syscall ?

il retourne l'adresse ou se trouve ces informations.